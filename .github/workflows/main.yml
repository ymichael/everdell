name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

  workflow_dispatch:

jobs:
  npm-ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup Node.js environment
        uses: actions/setup-node@v2.1.4
      - name: node_modules cache
        id: npm-ci-cache
        uses: actions/cache@v2
        with:
          path: |
            ./node_modules
            ~/.cache/Cypress
          key: ${{ runner.os }}-npm-ci-2-${{ hashFiles('**/package-lock.json') }}
      - name: npm ci
        if: steps.npm-ci-cache.outputs.cache-hit != 'true'
        run: npm ci

  unit-test:
    runs-on: ubuntu-latest
    needs: npm-ci
    steps:
      - uses: actions/checkout@v2
      - name: Setup Node.js environment
        uses: actions/setup-node@v2.1.4

      - name: node_modules cache
        id: npm-ci-cache
        uses: actions/cache@v2
        with:
          path: |
            ./node_modules
            .cache/Cypress
          key: ${{ runner.os }}-npm-ci-2-${{ hashFiles('**/package-lock.json') }}

      - name: Run prettier check
        run: npm run format:ci
      - name: Run lint check
        run: npm run lint
      - name: Run typecheck
        run: npm run typecheck
      - name: Run unit tests
        run: npm run test

  build:
    runs-on: ubuntu-latest
    needs: npm-ci
    steps:
      - uses: actions/checkout@v2
      - name: Setup Node.js environment
        uses: actions/setup-node@v2.1.4

      - name: node_modules cache
        id: npm-ci-cache
        uses: actions/cache@v2
        with:
          path: |
            ./node_modules
            ~/.cache/Cypress
          key: ${{ runner.os }}-npm-ci-2-${{ hashFiles('**/package-lock.json') }}

      - name: nextjs cache
        id: next-js-cache
        uses: actions/cache@v2
        with:
          path: ${{ github.workspace }}/.next/cache
          key: ${{ runner.os }}-nextjs-${{ hashFiles('**/package-lock.json') }}

      - name: nextjs build output
        id: next-js-build-cache
        uses: actions/cache@v2
        with:
          path: ${{ github.workspace }}/.next
          key: ${{ github.sha }}-nextjs

      - name: Run build
        if: steps.next-js-build-cache.outputs.cache-hit != 'true'
        run: npm run build:ci

  cypress-test:
    runs-on: ubuntu-latest
    needs: [npm-ci, build]
    steps:
      - uses: actions/checkout@v2
      - name: Setup Node.js environment
        uses: actions/setup-node@v2.1.4

      - name: node_modules cache
        id: npm-ci-cache
        uses: actions/cache@v2
        with:
          path: |
            ./node_modules
            ~/.cache/Cypress
          key: ${{ runner.os }}-npm-ci-2-${{ hashFiles('**/package-lock.json') }}

      - name: nextjs build output
        id: next-js-build-cache
        uses: actions/cache@v2
        with:
          path: ${{ github.workspace }}/.next
          key: ${{ github.sha }}-nextjs

      - name: Start server
        run: |
          PORT=3000 npm run start &
          sleep 5 && 
          curl http://localhost:3000/api/status -I

      - name: Cypress run
        uses: cypress-io/github-action@v2.7.0
        with:
          record: true
          quiet: true
          # we have already installed all dependencies above
          install: false
        env:
          DB_PATH: ./db/game.sqlite
          CYPRESS_RECORD_KEY: ${{ secrets.CYPRESS_RECORD_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
